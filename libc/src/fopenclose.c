#include "stdio.h"

FILE * fopen(const char* file, const char* mode)
{
  /* Mode is ignored for now. */
  /* TODO : need malloc() ! */
  return 0x0;
}

void fclose(FILE * file)
{
    /* Nope */
    return;
}
